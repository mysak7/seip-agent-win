AgentPath: "C:\ProgramData\SEIP"
ToolsPath: "C:\ProgramData\SEIP\.tools"
LuaWatcherSchedule: "*/5 * * * *"  # every 5 min, aligned to :07/:12/:17/... (minute % 5 == 2)
AllowStartWithoutLatestFilters: false  # if true, start with cached filters when bundle download/verify fails; if false, abort startup
